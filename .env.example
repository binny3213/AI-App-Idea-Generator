# Copy to .env locally. On hosting (Render, Railway, etc.), set these in the dashboard — never commit .env.
#
# If generation fails with "quota" or 429: add billing/credits at https://platform.openai.com/account/billing

OPENAI_API_KEY=sk-your-key-here
PORT=3000
